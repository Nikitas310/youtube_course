# א
- נתונה רשימת מספרים שלמים `nums`. (אפשר לקלוט אותה דרך `input` או פשוט לרשום בתוך הקוד, בתרגיל הזה זה לא משנה)
- עליכם יש לספור את כמות האיברים החיוביים, ולסכום את כל המספרים השליליים ברשימה.
- הדפיסו את התוצאות.

```python
nums = []

non_neg_count = 0
neg_sum = 0

if not nums:
    return []
    
for num in nums:
    if num > 0:
        non_neg_count += 1
    else:
        neg_sum += num
```

# ב 
- נתון מספר שלם חיובי, המירו את המספר לבסיס בינארי והדפיסו.

*הערה:* 


```python
a = 3  
a_bin = ''  
  
while a > 0:  
    a_bin = str(a % 2) + a_bin  
    a = a // 2  
  
print(a_bin)
```


# ג
- הגדירו משתנה `n` וקלטו לתוכו מספר שלם אי-שלילי.
- הגדירו רשימה `mat` ושמרו בתוכה **מטריצת היחידה** בגודל `n`.

*הערות:* 
1) מטריצת בגודל `n` בפייתון היא רשימה דו-מימדית עם `n` איברים, כך שכל איבר הוא רשימה עם `n` איברים.
2) מטריצת היחידה בגודל `n` היא מטריצה כך שכל האיברים שלה הם אפסים חוץ מאיברי האלכסון הראשי (מקצה השמאלי העליון לקצה הימני התחתון).
3) מטריצה בגודל $0$ היא רשימה ריקה.

*מידע נוסף על מטריצת היחידה ניתן למצוא כאן:* [קישור](https://he.wikipedia.org/wiki/%D7%9E%D7%98%D7%A8%D7%99%D7%A6%D7%AA_%D7%94%D7%99%D7%97%D7%99%D7%93%D7%94)

**דוגמאות קלט ופלט:**
<style>
  table {
    border-collapse: collapse;
    margin: 1em 0;
  }

  th, td {
    border: 1px solid #ddd;
    padding: 8px 12px;
    text-align: center;
  }
</style>

| n   | mat       |
| --- | --------- |
| 0   | `[]`      |
| 1   | `[[1]]`   |
| 2   | `[[1,0],` |
| 5   |           |


